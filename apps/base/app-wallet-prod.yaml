apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: svc-wallet-prod
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: exchange
    env: prod
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  project: platform
  source:
    repoURL: https://github.com/2025-demo-01/svc-wallet.git
    targetRevision: main
    path: k8s/overlays/prod               # ← prod overlayy
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
